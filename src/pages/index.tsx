import { useEffect, useContext, FC } from "react";
import {Layout} from "@/components/Layout";

const Home: FC = () => {
	return (
		<>
			<Layout>
				<h1>Home Page</h1>
			</Layout>
		</>
	);
};

export default Home;
